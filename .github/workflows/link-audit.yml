name: Weekly Link Audit

on:
  schedule:
    # 매주 화요일 00:30 UTC = 09:30 KST
    - cron: '30 0 * * 2'
  workflow_dispatch:

jobs:
  audit:
    name: Broken links & orphan pages
    runs-on: ubuntu-latest
    # Builds the whole site first. The 08-11 pass took 11m49s, leaving barely
    # three minutes of headroom at the old 15. Same budget as ci.yml.
    timeout-minutes: 40

    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Setup Node
        uses: actions/setup-node@v6
        with:
          node-version: 20
          cache: npm

      - name: Install
        run: npm ci

      - name: Build
        run: npm run build

      - name: Link audit
        run: python3 scripts/link-audit.py dist >> "$GITHUB_STEP_SUMMARY"

      - name: i18n residue audit
        run: |
          NS=$(python3 -c "import json; print(','.join(json.load(open('src/locales/ko.json')).keys()))")
          python3 scripts/i18n-residue-audit.py dist "$NS" >> "$GITHUB_STEP_SUMMARY"
