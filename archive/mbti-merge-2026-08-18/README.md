# wiki MBTI 격자 81편 → oiyo 통합 — 2026-08-18

> 짝 커밋: wiki `e6d53cc` ↔ oiyo `8ddc584`.
> ⚠️ wiki 커밋 메시지 본문은 oiyo 해시를 `8f4b0e0` 으로 적고 있는데 **틀렸다.**
> 커밋하기 전에 상대 해시를 안다고 가정하고 쓴 값이다. 정본은 이 줄이다.

세운 결정: **MBTI 는 oiyo 가 정본.** 노출 수로 고르지 않고 도메인 계약(oiyo=실행)대로 정했다.
`git rm` 이 아니라 `git mv` 다. 판단이 뒤집히면 되돌리면 된다.

## 무엇이 있었나 — 16×5 조합격자

| 세트 | 편수 | 평균 본문 | 처리 |
|---|--:|--:|---|
| `mbti-dict-*` — 유형 사전 | 16 | 682자 | 301만 (oiyo 타입 페이지가 이미 더 두껍다) |
| `mbti-career-*` — 직업 추천 | 16 | 1,296자 | oiyo `직업 적성` 섹션으로 흡수 |
| `mbti-compat-*` — 궁합 | 16 | 1,196자 | oiyo `관계와 연애` 섹션으로 흡수 |
| `mbti-love-*` — 연애 스타일 | 16 | 1,177자 | oiyo `관계와 연애` 섹션으로 흡수 |
| `mbti-study-*` — 공부법 | 16 | 1,211자 | oiyo `공부법` 섹션으로 흡수 |
| `mbti-matrix` — 격자 허브 | 1 | 3,544자 | 301 (격자가 사라지면 색인할 대상이 없다) |
| 합계 | **81** | | |

## 근거 — GSC 실측 (2026-05-18 ~ 2026-08-15, 90일)

MBTI 는 세 사이트에 전부 있었고, **셋 다 클릭이 0이었다.**

| | 페이지 | 노출 | 클릭 |
|---|--:|--:|--:|
| wiki | 94 | 18 | 0 |
| blog | 97 | 44 | 0 |
| oiyo | 76 | 91 | 0 |
| 합계 | **267** | 153 | **0** |

267개 페이지가 같은 질의를 놓고 서로 경쟁해 다 같이 졌다. 같은 기간 wiki 전체는 4,567 URL 에 클릭 1회다.

## 왜 blog 로 옮기지 않았나

`직업 추천`·`궁합`·`공부법`은 성격상 "설명"이라 도메인 계약으로는 blog 몫이다.
그런데 그렇게 하면 **16×3 격자를 blog 로 이동시킬 뿐 격자 자체는 그대로 남는다.**
이 형태(주제 하나를 조합으로 쪼개 URL 을 늘리는 것)는 2026-07-14 크롤 예산 반전 판정에서
줄여야 할 대상으로 이미 지목됐다. 그래서 이동이 아니라 **oiyo 타입 페이지 16개로 흡수**했다.
결과: 81 URL → 0 URL, oiyo `/ko/mbti/{type}` 는 32.6KB → 46.8KB 로 두꺼워졌다.

## 흡수 방식 — 한국어 전용이다

`oiyo/src/lib/engines/interpretation/shards/mbti-life-ko.ts` 로 추출했다.

⚠️ **원문이 ko 로만 존재했고 기계번역을 만들지 않기로 했다.** 따라서 이 데이터는
`SixLangString` 이 아니고, `[type].astro` 는 `locale === 'ko'` 일 때만 렌더한다.
빌드 산출물로 확인했다 — ko 16/16 페이지에 세 섹션이 나오고, en·ja·zh·fr·es 에는 0건이다.

원문의 템플릿 버그도 함께 고쳤다: 삽입어 뒤 조사가 받침을 무시해
`냉소적 태도이 반복될` 처럼 나오던 것 26건 (`mbti-compat-*`·`mbti-study-*`).

## 링크 재지정 — 115곳

- `meaning-of-mbti-types.mdx` × 6로케일, 각 16링크 → **각자 로케일의** oiyo 타입 페이지.
  기존에는 en·ja·zh·fr·es 판이 전부 `/ko/mbti-dict-*` 를 가리켜 **영어 독자를 한국어 페이지로
  보내고 있었다.** 이번 재지정으로 그 버그도 사라진다.
- `src/data/term-catalogs.json` 16링크 → oiyo (JSON 파싱 재확인)
- `enneagram-dict-type{1,4,7}.mdx` 각 1링크 → oiyo

재지정 후 격자 슬러그 실참조는 0이다(슬러그 경계 정규식으로 전 소스 확인).
⚠️ 처음 쓴 확인 정규식은 `mbti-love-` 가 `mbti-love-test` 에도 걸려 오탐 5건을 냈다.
슬러그 확인은 `\b` 경계를 넣어야 한다.

## 남긴 것

`meaning-of-mbti.mdx` 6로케일(1,662~3,672자)은 **남겼다.** MBTI 라는 개념 자체의 정의이고,
그건 wiki 의 역할이다. `meaning-of-mbti-types.mdx` 6로케일도 개요로 남기고 링크만 돌렸다.

## 리다이렉트

`public/_redirects` 에 163줄(81 slug × 슬래시 유무 + 주석). 557 → 719 규칙, 한도 2,000.
컷오프는 blog 고유이고 wiki 에는 없다 — `cloudflare-pages-rules-pitfalls-2026-07-16.md` 참조.

⚠️ **배포 후 라이브 curl 검증이 남아 있다.** 위 문서의 규칙 3·6이 요구한다.
빌드·dist 확인은 규칙이 파싱된다는 것을 보증하지 않는다. 파일 하단 규칙 하나를 반드시 찍어볼 것.
