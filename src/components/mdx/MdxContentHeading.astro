---
interface Props {
  id?: string;
}

const { id } = Astro.props;
---

<!-- The route owns the document-level h1. MDX-authored `#` headings start the
     article outline beneath that canonical title. -->
<h2 id={id}><slot /></h2>
